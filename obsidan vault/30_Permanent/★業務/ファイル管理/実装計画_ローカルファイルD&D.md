# 実装計画：デスクトップ・ExplorerからのローカルファイルD&D入力

## 1. 文書情報

- 対象：OneDrive業務フォルダ向け「保存先レコメンダー」
- 位置付け：MVP 0運用開始後の小改修（Outlook直接D&Dとは区別する）
- 作成日：2026-08-02
- 基準ブランチ：`ferch1_保存先レコメンダー修正`
- 基準コミット：`5aafacb`
- 状態：Step 5完了・実装レビュー待ち

この文書は実装方法と各Stepの結果を記録する計画書である。Step 2では要件定義書と既存テストだけを更新し、プログラム本体、依存関係、READMEはまだ変更しない。

---

## 2. 目的と対象範囲

WindowsデスクトップまたはExplorerに存在するファイルを、保存先レコメンダーの入力欄へドラッグ＆ドロップ（以下、D&D）して投入できるようにする。

今回扱うもの：

- デスクトップまたはExplorer上のローカルファイル
- Explorerで複数選択したファイル
- ローカルに保存済みのMSG 1件
- PDF、Office、画像、図面等の通常ファイル1件または複数
- 日本語、空白、全角・半角括弧を含むパス

今回扱わないもの：

- Outlook画面に表示されたメールの直接D&D・直接取得
- フォルダの投入・走査
- アプリからExplorerへのドラッグ
- ファイルのコピー、移動、削除、名前変更
- Windowsショートカット（`.lnk`）のリンク先解決
- 複数MSG、MSGと通常ファイルの混在
- D&D専用画面、設定、ログ、履歴、アニメーション

Outlook画面からの直接D&Dは、実ファイルのパスを渡すデスクトップ・ExplorerのD&Dとはデータ形式が異なるため、別改修とする。

---

## 3. 操作仕様

- 既存の入力ファイル一覧をドロップ先とし、新しい画面は設けない
- 入力欄の近くに「デスクトップまたはExplorerからファイルをここへドロップできます」と常設表示する
- ドロップされたファイル群を新しい1案件として扱い、現在の入力へ追記せず置き換える
- 新入力の処理に成功した場合だけ、入力、検索語、候補一覧を置き換える
- 失敗した場合は、直前の入力、検索語、候補一覧、確定パスを維持する
- 「ファイルを選択」ボタンを残し、D&Dが使えなくても従来操作を継続できるようにする

既存の入力規則を変更しない。

- MSGは1件だけ受け付ける
- MSG以外の複数ファイルは1案件として扱う
- 複数MSG、MSGと通常ファイルの混在は、既存の案内で全件拒否する
- フォルダが1件でも含まれる場合も全件拒否し、受け付け可能なファイルだけを抜き出さない
- ファイル名、MSG本文・件名・添付名からの検索語生成は変更しない

成功後の表示、確定パスのクリア、検索欄へのフォーカス、エラー案内は、現在のファイル選択と同じ処理を使う。

---

## 4. 安全性と不変条件

D&DはOSからローカルファイルのパスを受け取るだけとし、次を行わない。

- 元ファイルまたはOneDriveへの書き込み
- ファイルのコピー・移動・削除
- 外部サービスへの送信
- 保存先の確定またはクリップボードへのコピー
- D&D操作自体のAudit記録

実業務ファイル名と絶対パスは、既存の入力一覧表示とメモリ上の入力状態だけで扱う。D&D専用ログは作らない。

---

## 5. 技術方式

### 5.1 依存

`tkinterdnd2==0.6.2`をWindows限定の実行時依存として追加する案を第一候補とする。

- 既存の`tk.Tk()`は置き換えず、ルート作成後に`TkinterDnD.require(root)`を呼ぶ
- importとTk拡張の読込みはD&D初期化時まで遅延させる
- パッケージは`pyproject.toml`でバージョンを固定する
- 未導入または初期化失敗時はD&Dだけを無効化し、アプリとファイル選択を残す
- Windows以外ではD&Dを初期化せず、既存のヘッドレステストを維持する

現行版0.6.2には既存ルートへ適用する`TkinterDnD.require()`が用意されている。ただしバイナリを含む新規依存なので、採用は§7の実機検証に合格した場合だけ確定する。

通常の`pip install -e ".[dev]"`だけで個人利用環境へD&D機能を導入できるよう、optional dependencyには分けない。初期化失敗時のフォールバックは、インストール手順を増やすためではなく、アプリ全体の起動不能を防ぐために残す。

Win32の`WM_DROPFILES`をTkinterへ直接組み込む方式は、ウィンドウプロシージャの差し替え、64bit対応、終了時の解除等の独自管理が必要になるため採用しない。`tkinterdnd2`が不適合でも自動的にこの方式へ切り替えず、改めて判断する。

### 5.2 パス解析と既存処理の共用

ドロップイベントの生文字列を空白で`split()`せず、`root.tk.splitlist(event.data)`を使う。空白、日本語、括弧を含む単一・複数パスを変更しないためである。

`app.py`の現在の`_select_files()`から、選択済みパスを処理する部分を共通メソッドへ抽出する。

```text
ファイル選択 → パス取得 ─┐
                          ├→ _accept_files(paths) → session.select_files(paths)
D&D → splitlist ──────────┘
```

D&D側に入力判定を再実装せず、MSGとフォルダの規則は既存の`RecommenderSession.select_files()`へ集約する。`select_files()`はパス解決直後に各項目が既存ファイルであることを確認し、フォルダまたは存在しないパスを1件でも含む場合は`InputSelectionError`で全件拒否する。シェルコマンド文字列は組み立てない。

---

## 6. 想定する変更ファイル

| ファイル | 変更内容 |
|---|---|
| `pyproject.toml` | Windows限定で固定した`tkinterdnd2`依存を追加 |
| `src/onedrive_destination_recommender/app.py` | 遅延初期化、ドロップ処理、既存入力処理の共通化、常設案内 |
| `src/onedrive_destination_recommender/session.py` | フォルダ・存在しないパスの拒否を既存入力規則へ追加 |
| `tests/unit/test_session.py` | フォルダ拒否を1件追加し、既存テストの入力用パスに一時ファイルを作る |
| `tests/integration/test_gui.py` | パス解析、入力経路の同一性、フォールバックのテスト |
| `README.md` | デスクトップ・Explorerからの操作と対象外を追記 |
| `要件定義_OneDrive保存先レコメンダー.md` | §6、§15、§20、§22へ反映 |
| 本計画書 | 実装・検証結果を追記 |

新規Pythonモジュール、新規テストファイル、設定項目、ログファイルは追加しない。

---

## 7. 実装前の依存検証（Step 0）

プログラム本体を変更する前に、現在のWindows・Python 3.12環境と一時領域のダミーファイルで次を確認する。

1. `tkinterdnd2==0.6.2`を導入し、既存の`tk.Tk()`へ`TkinterDnD.require(root)`を適用できる
2. デスクトップから単一ファイル、Explorerから複数ファイルの正しいパスを取得できる
3. 日本語、空白、括弧を含むパスを変更せず取得できる
4. 画面終了後にPythonプロセスが残らない
5. 既存のファイル選択ボタンとExplorer Previewが動作する
6. 現在の入力一覧が実際にドロップしやすい広さか確認する
7. `.lnk`はリンク先を解決せず、ショートカットファイル名が入力になることを確認する

### 7.1 実施結果（2026-08-02）

Windows・Python 3.12環境で、一時領域とデスクトップに作成したダミーファイルだけを使って確認した。結果は7項目すべて合格した。

| # | 結果 |
|---|---|
| 1 | `.venv`へ一時導入した`tkinterdnd2==0.6.2`を、既存の`tk.Tk()`へ`TkinterDnD.require(root)`で適用できた。読み込まれたtkdndは2.10.1だった |
| 2 | Explorerから通常ファイル2件、デスクトップからPDF 1件をドロップし、それぞれ正しい絶対パスを取得できた |
| 3 | `カメラ 仕様書（秋田）.docx`、`図面 [Rev1].pdf`等で、日本語、空白、全角・半角括弧を含むパスを変更せず取得できた。Tkの`splitlist()`による複数パスの往復比較も一致した |
| 4 | 検証画面を閉じるとPythonプロセスは終了コード0で終了し、検証用プロセスは残らなかった |
| 5 | 既存テストは90件合格・3件選択除外、Windows GUI結合テストは1件合格し、既存のファイル選択とExplorer Previewが継続して動作した |
| 6 | 現在の`input_list`と同じ`height=3`の検証欄は、複数ファイルをドロップする領域として支障のない広さだった。親フレームへ対象を広げる追加実装は不要と判断した |
| 7 | デスクトップ上の実ショートカットをドロップすると、リンク先ではなく`D:/Desktop/ODR_DnD_Step0_設備ショートカット_20260802.lnk`が渡された |

検証終了後、一時ファイルとデスクトップ上のダミー2件を削除し、検証用に一時導入した`tkinterdnd2`も`.venv`から削除した。リポジトリのプログラム本体と`pyproject.toml`は変更していない。

以上から、`tkinterdnd2==0.6.2`を§5.1の方式で採用し、Step 2以降へ進める。実装着手前に、本結果と§8.1の影響範囲修正について再レビューを受ける。

失敗した場合は、プログラム本体と`pyproject.toml`を変更せず、結果を本計画書へ記録して再レビューを依頼する。

### 7.2 Step 2実施結果（2026-08-03）

再レビュー済みの`a718b02`を基準に実施した。

要件定義書の§2.3、§6.1、§15.1、§20、§22を更新し、初期MVPでD&Dを対象外とした履歴を残したまま、ローカルファイルD&DをMVP 0運用後の小改修として定義した。§23の初期MVP対象外記録と、Outlook画面からのメール直接D&Dの対象外方針は変更していない。

既存テストは次のとおり更新した。

- セッション単体テスト7件の入力を、各テストの目的を保ったまま実在ファイル前提へ変更した
- 存在しないMSGの期待例外を`InputSelectionError`へ変更し、不要になったモックを削除した
- 選択後に通常ファイルが消えるテストは、選択成功後に一時ファイルを削除する順序へ変更した
- フォルダを含む入力の全件拒否と、直前のセッション状態維持を固定する単体テストを追加した
- GUI結合テストへ、パスの`splitlist()`、ファイル選択との結果一致・副作用なし、D&D初期化失敗時のフォールバックの3件を追加した
- Windows GUIテストの複数実行でTkインタープリタを重複生成しないよう、1つのTkルートを共有して各画面を分離する構成にした

プログラム本体、`pyproject.toml`、READMEは変更していない。失敗先行テストの実行結果は次のとおりで、既存挙動の回帰ではなく、Step 3で追加する予定の経路だけが失敗した。

| 確認 | Step 2時点の結果 |
|---|---|
| ヘッドレステスト | 89件合格、2件失敗、6件選択除外。失敗は存在しないMSGがまだ`FileNotFoundError`になることと、フォルダ入力をまだ拒否しないことの2件 |
| Windows GUI結合テスト | 既存1件合格、追加3件失敗。失敗は`_on_drop()`が未実装の2件と、D&D依存の遅延初期化が未実装の1件 |
| Ruff lint | 合格 |
| Ruff format check | 22ファイルすべて合格 |

以上から、Step 2は完了とする。Step 3ではこの5件の失敗を、§5の最小実装だけで合格へ変える。

### 7.3 Step 3実施結果（2026-08-03）

§5の最小実装だけを反映した。

- `tkinterdnd2==0.6.2`をWindows限定の実行時依存として追加し、通常の開発用インストールで導入できることを確認した
- Windowsの場合だけD&D依存を遅延importし、既存のTkルートへ適用した。未導入・読込み失敗時は例外を外へ出さず、ファイル選択を残す
- 既存の入力一覧をドロップ先にし、`<<Drop>>`を実際に登録できることをWindows GUI結合テストで確認した
- ドロップイベントのパスは`root.tk.splitlist()`で解析し、ファイル選択と同じ`_accept_files()`へ渡す構成にした
- `select_files()`のパス解決直後に`is_file()`を適用し、フォルダまたは存在しないパスを含む入力を、状態変更前に全件拒否するようにした
- 入力一覧の下へ「デスクトップまたはExplorerからファイルをここへドロップできます」という常設案内を追加した

Step 3時点の自動確認結果：

| 確認 | 結果 |
|---|---|
| ヘッドレステスト | 91件合格、6件選択除外 |
| Windows GUI結合テスト | 4件合格。`<<Drop>>`の登録、パス解析、入力経路の一致、初期化失敗時のフォールバックを確認 |
| 全結合テスト | 5件合格、実MSGパス未設定の1件だけ選択除外 |
| Ruff lint・format check | 22ファイルすべて合格 |

READMEと受け入れ条件の最終結果はまだ更新しない。Step 4でWindowsデスクトップ・Explorerからの実操作を確認した後、Step 5で記録する。

### 7.4 Step 4実施結果（2026-08-03）

Windowsの通常権限でアプリを起動し、デスクトップ・Explorer上の既存ファイルを使って手動確認した。実業務ファイル名と絶対パスは本記録へ残していない。

| 確認 | 結果 |
|---|---|
| デスクトップからPDF 1件 | 入力一覧、主検索語、候補が更新され、1案件として処理できた |
| Explorerから通常ファイル2件 | 2件を1案件として受理し、検索語と候補を再計算できた |
| 保存済みMSG 1件 | MSG解析を完了し、主検索語・補助検索語と候補を表示できた |
| フォルダの投入 | 全件拒否し、直前のMSG入力、検索語、候補を維持した |
| MSGと通常ファイルの混在 | 全件拒否し、直前の入力状態を維持した |
| 複数MSG | 全件拒否し、直前の入力状態を維持した |
| Explorer Preview | D&D後の候補と同じ既存フォルダをExplorerで開けた。開くだけでは確定しなかった |
| 確定・コピー後の再投入 | 確定、クリップボードコピー、Audit記録の後もD&Dを利用でき、新入力の成功時に前の確定パスをクリアした |

手動確認後の回帰確認は、ヘッドレステスト91件合格・6件選択除外、結合テスト5件合格・実MSGパス未設定の1件だけ選択除外、Ruff lint合格、Ruff format check 22ファイル合格だった。

以上から、§8.2のWindows手動確認6項目と§8.3の受け入れ条件のうち実操作で確認する項目は合格とする。README、要件定義書、受け入れ条件の最終記録はStep 5で更新した。

### 7.5 Step 5実施結果（2026-08-03）

実装コードを変更せず、次の最終文書更新を行った。

- READMEへD&Dの使い方、受け入れ結果、通常権限での起動、失敗時のファイル選択フォールバックを追記した
- READMEへOutlook画面からの直接D&Dが対象外であることと、`.lnk`のリンク先を解決しないため投入対象外とすることを追記した
- 要件定義書§20へ、ローカルファイルD&Dの受け入れ条件5件が合格したことと、自動・手動確認結果を追記した
- 本計画書の状態をStep 5完了へ更新し、Step 4・5の結果を実業務ファイル名や絶対パスを残さず記録した

Step 4後の最終確認結果は、ヘッドレステスト91件合格・6件選択除外、結合テスト5件合格・実MSGパス未設定の1件だけ選択除外、Ruff lint合格、Ruff format check 22ファイル合格だった。§8.3の受け入れ条件5件と§13の完了条件を満たしたため、Step 5は完了とし、実装レビューへ進める。

---

## 8. 検証計画と受け入れ条件

### 8.1 自動テスト4項目

既存のGUI結合テストへ3項目、既存のセッション単体テストへ1項目を追加し、新しいテストファイルは作らない。

1. GUI：`splitlist`で空白、日本語、括弧を含む単一・複数パスを、順序と内容を変えず共通処理へ渡す
2. GUI：D&Dとファイル選択が同じ入力状態、検索語、候補を生成し、D&DだけではAudit、クリップボード、確定パスを変更しない
3. GUI：D&D初期化に失敗しても画面が起動し、ファイル選択を利用できる
4. 単体：`select_files()`がフォルダを含む入力を全件拒否し、直前の入力状態を維持する

複数MSGとMSG・通常ファイル混在の拒否は既存の`test_session.py`で固定済みのため追加しない。

`select_files()`を呼ぶ既存のセッション単体テストは7件あり、現状は7件すべてが入力ファイルの実体を作らずパスだけを渡している。`is_file()`追加後も各テストの目的を変えないよう、次のとおり直す。

- 7件の入力パスは、原則として`tmp_path`に空の一時ファイルを作ってから渡す
- 「存在しないMSGで現在の入力を維持する」テストは、`build_msg_search_terms`のモックを外し、期待する例外を`FileNotFoundError`から`InputSelectionError`へ変更する
- 「選択後に通常ファイルが消えてもCodex相談を続けられる」テストは、`select_files()`の成功後に一時ファイルを削除する
- 実装時に追加するGUI結合テストでファイル選択経由の`select_files()`を通す場合も、入力時点で一時ファイルを用意する

ネイティブD&DイベントとOSの接続は、ヘッドレス環境で再現せず、Step 0とWindows手動確認で検証する。全既存テスト、Windows結合テスト、Ruff lint・format checkも実行する。

### 8.2 Windows手動確認

- デスクトップからPDF 1件を投入する
- Explorerから通常ファイル複数件を1案件として投入する
- 保存済みMSG 1件を投入する
- フォルダ、複数MSG、MSGと通常ファイルの混在を拒否する
- D&D後もExplorer Previewと確定・コピーを利用できる
- 通常権限で起動し、管理者権限での起動を前提にしない

### 8.3 受け入れ条件5件

1. WindowsデスクトップとExplorerの双方からローカルファイルを入力欄へドロップできる
2. 空白、日本語、括弧を含む単一・複数パスを変更せず受け取れる
3. MSG 1件、またはMSG以外の複数ファイルを、ファイル選択時と同じ規則・結果で処理でき、D&DだけではAudit、クリップボード、確定パスを変更しない
4. フォルダ、複数MSG、MSGと通常ファイルの混在を全件拒否し、直前の入力状態を維持できる
5. D&D初期化に失敗してもアプリを終了せず、従来のファイル選択で利用を継続できる

既存の候補判定、Explorer Preview、確定・コピー、Codex相談、Auditは既存テストで担保し、この小改修で二重管理しない。

---

## 9. 実装順序

1. Step 0で依存と実D&Dを検証し、結果を本計画書へ記録する
2. 要件定義書、既存GUI結合テスト、セッション単体テストを更新する
3. Windows限定依存、遅延初期化、共通入力処理、セッション層のフォルダ拒否、常設案内を最小実装する
4. 全テスト、Ruff、Windows手動確認を実施する
5. README、要件定義書、本計画書へ結果を記録し、実装レビューを依頼する

---

## 10. 主なリスクと対策

| リスク | 対策 |
|---|---|
| 新規のTkバイナリ依存が動かない | Step 0を先に行い、失敗時は本体を変更しない |
| パッケージ未導入でアプリ全体が起動しない | 遅延読込みとフォールバックでボタン入力を残す |
| 空白入りパスを複数項目と誤認する | Tkの`splitlist()`を使う |
| フォルダ配下を意図せず一括投入する | フォルダを含む場合は全件拒否する |
| D&Dとファイル選択で挙動が分かれる | パス取得後の処理を共通メソッド1本にする |
| 失敗した入力で現在の候補が消える | 成功した場合だけ表示状態を置き換える |
| 権限差でD&Dできない | 通常権限で起動する前提をREADMEへ記載する |
| Outlook D&Dまで対応したと誤解される | 画面とREADMEに「デスクトップまたはExplorer」と明記する |

---

## 11. 要件定義書の更新方針

- §6.1：ファイル選択に加え、デスクトップ・ExplorerからのローカルファイルD&Dを追加する
- §15：入力欄の案内とD&D失敗時のファイル選択フォールバックを追加する
- §20：MVP 0の16件とは別建てで、受け入れ条件5件と結果を記録する
- §22：「ファイルのD&D」を実装済み小改修として候補から外す
- §23の「ローカルファイルのD&D（MVP 0）」は初期MVPの対象外記録として正しいため変更しない
- Outlook画面からのメール直接D&Dは対象外のまま変更しない

---

## 12. 再レビューで確認してほしい点

1. `tkinterdnd2==0.6.2`をWindows限定・遅延読込みで使い、Step 0後に採用確定する判断は妥当か
2. Win32 `WM_DROPFILES`の独自実装を見送る判断は妥当か
3. ドロップ先を既存入力一覧だけにし、現在の入力を置き換える仕様でよいか
4. フォルダと存在しないパスの拒否を`session.select_files()`へ集約する方針でよいか
5. 受け入れ条件5件と自動テスト4項目に、削れる重複や不足がないか
6. Outlook直接D&DやD&D専用UIが今回の実装へ混入しない計画になっているか

---

## 13. 完了の定義

- Step 0と再レビューの双方で実装着手可と判定されている
- 受け入れ条件5件と既存テストがすべて合格している
- WindowsデスクトップとExplorerの実操作で単一・複数ファイルを投入できる
- D&DだけではAudit、クリップボード、確定パスが変化しない
- ファイル選択、手動検索、Explorer Preview、確定・コピーが継続して動作する
- Outlook画面からの直接D&Dを実装していない

---

## 14. 参考情報

- Microsoft Learn：[`DragAcceptFiles`](https://learn.microsoft.com/en-us/windows/win32/api/shellapi/nf-shellapi-dragacceptfiles)
- PyPI：[`tkinterdnd2`（2026-08-02確認時の現行版0.6.2）](https://pypi.org/project/tkinterdnd2/)
