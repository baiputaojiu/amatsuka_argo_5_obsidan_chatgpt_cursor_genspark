# {{prompt_id}} {{title}} — {{environment}}

## メタデータ
- template_version: {{template_version}}
- template_hash: {{template_hash}}
- run_id: {{run_id}}
- schema: {{schema_name}}

## 目的
{{purpose}}

## 使用タイミング
`NEXT_ACTIONS.md` がこの処理を指示したとき。実SOURCE IDは `analyst-forecast status {{run_id}}` で確認し、`<SOURCE_ID>` を推測しないこと。

## 推奨AI環境
{{environment}}

## 使用モデル
{{model_text}}
（廉価モデルへの自動切替は禁止。未設定なら設定コマンドを先に実行すること。）

## 入力ファイル
{{input_instruction}}

## 出力ファイル・Schema
`03_ai_outputs/inbox/{{output_name}}`。`01_prompts/schemas/{{schema_name}}` に厳密に従う。

## 判定基準（必須）
- 話者交代、司会者の質問、相づち、他出演者を混同しない。
- content author（番組側メタ）と statement speaker（実際の発言者）を混同しない。
- 調査対象者の過去発言や動画メタデータは根拠として記録し、後発情報は使わない。
- knowledge_cutoff は recorded_at（なければ published_at）以前に制限する。市場結果を渡さない。
- 判断不能は unknown / needs_review とし、無理に確定しない。

## 禁止事項・注意事項
{{warning}}

## 完了条件
入力、引用、判断根拠、未解決事項が出力に含まれる。

## コピペ用依頼文
この案件の{{title}}を実行してください。上記の禁止事項と出力先を厳守してください。
