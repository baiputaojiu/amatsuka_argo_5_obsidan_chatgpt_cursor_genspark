# {{prompt_id}} {{title}} — {{environment}}

## メタデータ
- template_version: {{template_version}}
- template_hash: {{template_hash}}
- run_id: {{run_id}}
- schema: {{schema_name}}

## 目的
{{purpose}}

## 使用タイミング
P08受理後、NEXT_ACTIONS が RUN_P11 を示したとき。実COMPONENT_IDは status で確認する。

## 推奨AI環境
{{environment}}

## 使用モデル
{{model_text}}

## 入力ファイル
{{input_instruction}}

## 出力ファイル・Schema
`03_ai_outputs/inbox/{{output_name}}`。`01_prompts/schemas/{{schema_name}}` に厳密に従う。

## 判定基準（必須）
- 原文明示対象を最優先。発言時点に存在した公式指数・ETF・固定basketのみ。
- 最大3候補。結果確認後の後知恵禁止。
- 別componentの提案を混ぜない。市場結果を入力しない。
- knowledge_cutoff <= forecast made_at。

## 禁止事項・注意事項
{{warning}}

## 完了条件
candidates または unresolvable 理由が揃う。

## コピペ用依頼文
この案件の{{title}}を実行してください。上記の禁止事項と出力先を厳守してください。
