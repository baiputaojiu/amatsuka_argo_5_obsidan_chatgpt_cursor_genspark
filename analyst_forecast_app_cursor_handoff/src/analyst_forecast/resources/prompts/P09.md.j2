# {{prompt_id}} {{title}} — {{environment}}

## メタデータ
- template_version: {{template_version}}
- template_hash: {{template_hash}}
- run_id: {{run_id}}
- schema: {{schema_name}}

## 目的
{{purpose}}

## 使用タイミング
needs_review の P08 があるとき。

## 推奨AI環境
{{environment}}

## 使用モデル
{{model_text}}

## 入力ファイル
{{input_instruction}}

## 出力ファイル・Schema
`03_ai_outputs/inbox/{{output_name}}`。`01_prompts/schemas/{{schema_name}}` に厳密に従う。

## 判定基準（必須）
- 否定誤読、条件欠落、本命誤認、時期・程度の捏造を重点確認。
- accept / correct / reject / unresolved。
- correct 時は P08 Schema の完全な corrected_payload。

## 禁止事項・注意事項
{{warning}}

## 完了条件
decision と findings、必要なら corrected_payload が揃う。

## コピペ用依頼文
この案件の{{title}}を実行してください。上記の禁止事項と出力先を厳守してください。
