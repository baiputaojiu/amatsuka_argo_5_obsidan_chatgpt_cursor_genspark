# {{prompt_id}} {{title}} — {{environment}}

## メタデータ
- template_version: {{template_version}}
- template_hash: {{template_hash}}
- run_id: {{run_id}}
- schema: {{schema_name}}

## 目的
{{purpose}}

## 使用タイミング
P11受理後。proposal_artifact_id と input_hash を実値で埋める。

## 推奨AI環境
{{environment}}

## 使用モデル
{{model_text}}

## 入力ファイル
{{input_instruction}}

## 出力ファイル・Schema
`03_ai_outputs/inbox/{{output_name}}`。`01_prompts/schemas/{{schema_name}}` に厳密に従う。

## 判定基準（必須）
- 同じ run/source/component の P11 だけをレビューする。
- 修正候補がある場合は corrected_candidate と recommended_candidate_origin=p12_correction。
- 後知恵禁止。市場結果を入力しない。unresolvable を許容。

## 禁止事項・注意事項
{{warning}}

## 完了条件
agreed / disagreed / unresolved のいずれかと根拠が揃う。

## コピペ用依頼文
この案件の{{title}}を実行してください。上記の禁止事項と出力先を厳守してください。
