# {{prompt_id}} {{title}} — {{environment}}

## メタデータ
- template_version: {{template_version}}
- template_hash: {{template_hash}}
- run_id: {{run_id}}
- schema: {{schema_name}}

## 目的
{{purpose}}

## 使用タイミング
needs_review の P05/P07 があるとき。reviewed_artifact_id と input_hash（対象output hash）を実値で埋める。

## 推奨AI環境
{{environment}}

## 使用モデル
{{model_text}}

## 入力ファイル
{{input_instruction}}

## 出力ファイル・Schema
`03_ai_outputs/inbox/{{output_name}}`。`01_prompts/schemas/{{schema_name}}` に厳密に従う。

## 判定基準（必須）
- decision は accept / correct / reject / unresolved のみ。
- correct 時は元Schemaの完全な corrected_payload を返す。
- 話者交代・司会者・他出演者・判断不能を重点確認する。
- 市場結果や後発情報を使わない。

## 禁止事項・注意事項
{{warning}}

## 完了条件
decision、findings、根拠、必要なら corrected_payload が揃う。

## コピペ用依頼文
この案件の{{title}}を実行してください。上記の禁止事項と出力先を厳守してください。
